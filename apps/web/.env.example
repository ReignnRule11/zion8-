# Public base URL of the web app
APP_BASE_URL=http://localhost:3000

# Internal URL used by the server to reach the API (rewrites and RSC fetches)
API_INTERNAL_URL=http://localhost:4000

# API version segment resolved to /api/v<API_VERSION>
API_VERSION=1
